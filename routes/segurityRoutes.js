// const { apiLimiter, authorizeRoles, checkUserBlock } = require('../middlewares/securityMiddleware');
// const securityController = require('../controllers/securityController');

// app.use('/api', apiLimiter);
// app.get('/api/security/logs', authorizeRoles('admin'), securityController.getSecurityLogs);
// app.use(checkUserBlock);